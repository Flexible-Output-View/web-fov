version: "3"

services:
    nginx:
        build: ./nginx
        ports:
            - 80:8080
            # - 80:80
            - 1935:1935
        restart: on-failure

    proto:
        build: ./proto
        ports:
            - 8081:8080 # Changement de port (site web de dev sur 8081 maintenant)
        restart: on-failure
